'use client';

import React from 'react';

/**
 * SalesToast: Kept as an empty/clean component until real automated order events are plugged in.
 * Prevents any artificial or mock purchase notifications.
 */
export function SalesToast() {
  return null;
}
